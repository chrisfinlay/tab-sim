"""Cross-version replay evidence: frozen PR #44 artefacts and #44's own reader.

Historical test code. Nothing under ``tabsim/`` may import anything from here;
these modules exist to answer "does the adopted implementation still agree with
the one that defined the format", which the adopted implementation cannot answer
about itself.
"""
